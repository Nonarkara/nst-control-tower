# SPA routes that share a path prefix with a static directory (public/data/…).
# Exact matches only — /data/workbench/*.json must still be served as files.
/data   /index.html   200
/data/  /index.html   200
