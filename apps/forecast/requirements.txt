# Web server + scheduler
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
apscheduler>=3.10.4

# PostgreSQL async driver
asyncpg>=0.29.0

# HTTP — used for the live Open-Meteo flood/precip forecast (always required)
httpx>=0.27.0

# TimesFM — Google Research time-series foundation model. OPTIONAL: the service
# degrades gracefully to Open-Meteo when this (and PyTorch) are not installed.
# Install separately on a GPU/large-RAM host to enable model forecasting:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install "timesfm[torch]>=1.2.0"
# timesfm[torch]>=1.2.0
